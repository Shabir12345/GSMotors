import React from 'react';

export const metadata = {
    title: 'Browse Our Inventory | Premium Pre-Owned Vehicles — GS Motors',
    description: 'Explore our curated selection of premium pre-owned vehicles. Certified quality, transparent pricing, and extensive 150-point inspections on all makes and models.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
