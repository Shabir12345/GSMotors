import React from 'react';

export const metadata = {
    title: "Export Solutions | Global Vehicle Shipping — GS Motors",
    description: "Seamless international vehicle logistics. We handle documentation, shipping coordination, and compliance for our premium export inventory.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
