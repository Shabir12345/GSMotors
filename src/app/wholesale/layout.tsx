import React from 'react';

export const metadata = {
    title: "Wholesale Direct | B2B Dealer Portal — GS Motors",
    description: "Join our exclusive network of certified dealers. Access premium inventory at wholesale pricing with competitive transfer rates.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
